# Novel2Gal Backend

Backend package for Novel2Gal MVP.

Current modules are deliberately dependency-light so the parser and exporter baseline can be tested before adding FastAPI, database, LlamaIndex, LangGraph, or pgvector integration.
